"""CLI helper tool 패키지.

클래스 구현은 각 tool 파일에 있고, 이 패키지는 calibration/debug script들을
`python -m tools.<tool_name>` 형태로 실행할 수 있게 묶는 역할을 한다.
주요 tool은 calibration, dataset debug, temporal-offset video debug이다.
"""
